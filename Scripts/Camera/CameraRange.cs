using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraRange : MonoBehaviour
{
    public void SetRange(float indicator)
    {
        //LeanTween.moveLocalZ(gameObject, 10 + indicator, .75f);
    }
}
